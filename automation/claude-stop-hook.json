{
  "_comment": "Merge the Stop block into %USERPROFILE%\\.claude\\settings.json. install.ps1 does this for you; this file is the reference copy. If a Stop array already exists, append this entry to it rather than replacing it.",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File \"%USERPROFILE%\\.obsidian-wiki\\mark-pending.ps1\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
